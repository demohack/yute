$('#next').on('click', function (e) {
    e.preventDefault();

    console.log("click : next")
})

$('#back').on('click', function (e) {
    e.preventDefault();

    console.log("click : back")
})

$('#submit').on('click', function (e) {
    e.preventDefault();
    // let next_question_id = 
    // $("#form").action = "/answer/" + next_question_id
    console.log("click : submit")
})
